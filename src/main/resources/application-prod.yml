spring:
  security:
    oauth2:
      client:
        registration:
          google:
            redirect-uri: https://camping-on.site/login/oauth2/code/google

app:
  front-url: http://localhost:3000

jwt:
  access-expired: 900 # 15분
  refresh-expired: 86400 # 1일